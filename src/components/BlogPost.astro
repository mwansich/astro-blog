---
const { url, title, date, image, alt, description, author } = Astro.props;
---
<div class="blog-thumbnail">
<a href={url}><img src={image} alt={alt} /></a>
<h2><a href={url}>{title}</a></h2>
<p>{date.toLocaleDateString()}</p>
<p>{description}</p>
</div>

<style>
    img {
        width: 100%;
    }
    h2 {
        margin-bottom: -12px;
    }

</style>