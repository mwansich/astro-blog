---
import '../styles/global.css';
import BaseLayout from '../layouts/BaseLayout.astro';

const pageTitle = "About Me";

const identity = {
  firstName: "M",
  country: "Germany",
  occupation: "Public Servant",
  hobbies: ["photography", "watching movies with Nicolas Cage"],
};

const skills = ["C", "HTML", "CSS", "JavaScript", "SQL", "Python", "Flask", "Astro"];

const skillColor = "purple";
const fontWeight = "bold";
const textCase = "uppercase"
---

 <style is:global define:vars={{skillColor, fontWeight, textCase}}>
            .skill {
                color: var(--skillColor);
                font-weight: var(--fontWeight);
                text-transform: var(--textCase);
            }

            .dark .skill {
                color: #8b5cf6;
            }
        </style>

<BaseLayout pageTitle={pageTitle}>
<h2>and my final CS50 Project</h2>
        <p>Here are a few facts about me:</p>
        <ul>
            <li>My name is {identity.firstName}.</li>
            <li>I live in {identity.country} and I work as a {identity.occupation}.</li>
        {identity.hobbies.length >= 2 ?
            <li>Two of my hobbies are: {identity.hobbies[0]} and {identity.hobbies[1]}</li> : 
            identity.hobbies.length === 1 ? <li>My only Hobby is: {identity.hobbies[0]}</li> :
            <li>I don't have any hobbies</li>
        }
        </ul>
        <p>I already learned the following skills in CS50:</p>
        <ul>
        {skills.slice(0, 6).map((skill) => <li>{skill}</li>)}
        </ul>
        
        <p>I'm now learning <span class="skill">{skills[7]}</span> while building this blog for my final project.</p>
</BaseLayout>
